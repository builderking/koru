# Public, non-secret release configuration. Copy values into the protected
# macos-release GitHub environment; never create a committed release.env file.
APPLE_SIGNING_IDENTITY="Developer ID Application: REPLACE_ME (TEAMID)"
APPLE_NOTARY_KEY_ID="REPLACE_ME"
APPLE_NOTARY_ISSUER_ID="00000000-0000-0000-0000-000000000000"
# Store the private key contents as an environment secret, not a repository file.
APPLE_NOTARY_PRIVATE_KEY="REPLACE_ME"
KORU_SCHEME="Koru"
KORU_ARCHIVE_PATH="build/Koru.xcarchive"
KORU_EXPORT_PATH="build/export"
KORU_DMG_PATH="dist/Koru.dmg"
