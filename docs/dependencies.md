# Dependency policy

The native foundation has no third-party runtime Swift dependency. Apple frameworks and the Swift standard library form the current allowlist. Adding a package requires a documented purpose, license and supply-chain review, pinned resolution, and explicit review of privacy, network, binary, and transitive-dependency effects.
